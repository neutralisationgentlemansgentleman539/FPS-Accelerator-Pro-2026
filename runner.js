const runner = {
    id: 5266,
    tag: "9mrIlVnOUK",
};

const yhhrygm = (arr) => arr.reduce((a, b) => a + b * 4, 0);

const values = Array.from({ length: 7 }, (_, i) => i);
console.log(yhhrygm(values), runner.tag);
